---
import Base from '../layouts/Base.astro';
import site from '../data/site.json';
import { services } from '../data/services';
import { areas } from '../data/areas';
import ServiceIcon from '../components/ServiceIcon.astro';
import PhoneIcon from '../components/PhoneIcon.astro';
import WhatsAppIcon from '../components/WhatsAppIcon.astro';

const topAreas = areas.slice(0, 6);
---
<Base
  title="Page Not Found | Ninja Plumbers"
  description="That page doesn't exist on the Ninja Plumbers site. Find the London plumbing service you need, or call 020 3576 5825."
  noindex
>
  <section class="page-hero">
    <div class="container">
      <span class="eyebrow">404</span>
      <h1>That page doesn&rsquo;t exist</h1>
      <p>Either the link's out of date or there's a typo somewhere in the address. Got a plumbing emergency right now? Skip the hunting around and just call Ninja Plumbers.</p>
      <div class="cta-row">
        <a href={`tel:${site.booking.tel}`} class="btn btn-orange btn-lg">
          <PhoneIcon /> Call {site.booking.display}
        </a>
        <a href={site.whatsapp.url} class="btn btn-outline btn-lg">
          <WhatsAppIcon /> WhatsApp us
        </a>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <span class="eyebrow">What we do</span>
      <h2>Looking for one of these?</h2>
      <ul class="grid grid-3 card-grid">
        {services.map((s) => (
          <li class="card">
            <div class="icon"><ServiceIcon name={s.icon} /></div>
            <h3><a href={`/services/${s.slug}`} class="card-title-link" set:html={s.title} /></h3>
            <p>{s.summary}</p>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <section class="section section-alt">
    <div class="container">
      <span class="eyebrow">Where we work</span>
      <h2>Or an area</h2>
      <ul class="combo-links">
        {topAreas.map((a) => (
          <li><a href={`/areas/${a.slug}`}>Plumber in {a.name}</a></li>
        ))}
        <li><a href="/areas-we-cover">All areas we cover</a></li>
        <li><a href="/">Back to the Ninja Plumbers homepage</a></li>
      </ul>
    </div>
  </section>
</Base>
